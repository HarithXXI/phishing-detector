{
  "name": "phishguard-v3.1",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"cd backend && uvicorn app.main:app --reload --reload-dir app --port 8000\" \"npm run dev --prefix frontend\"",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --reload-dir app --port 8000",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install:all": "pip install -r backend/requirements.txt -r backend/requirements-dev.txt && npm install --prefix frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
