{
  "name": "phishguard-v3.1",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"uvicorn backend.app.main:app --reload --reload-dir backend --port 8000\" \"npm run dev --prefix frontend\"",
    "dev:backend": "uvicorn backend.app.main:app --reload --reload-dir backend --port 8000",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install:all": "pip install -r backend/requirements.txt -r backend/requirements-dev.txt && npm install --prefix frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
